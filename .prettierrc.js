module.exports = {
  arrowParens: 'avoid',
  bracketSpacing: true,
  singleQuote: true,
  trailingComma: 'es5',
  semi: true,
  bracketSameLine: false,
  // Set the line length to 80 characters
  printWidth: 80,
  // Use single quotes instead of double quotes for strings
  singleQuote: true,
  // Add a trailing comma to the last item in an object or array
  // Use tabs instead of spaces for indentation
  useTabs: false,
  // Indent with 2 spaces
  tabWidth: 2,
  // Don't wrap markdown text at 80 characters
  proseWrap: 'never',
  parser: "flow"
};
