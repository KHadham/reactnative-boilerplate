import { widthByScreen } from '@helper/dimensions';
import { StyleSheet } from 'react-native';
import { widthByScreen } from '../../../helper/dimensions';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor:'white'
    // justifyContent: 'center',
  },
  image: {
    width: '100%',
    height: '100%',
  },
});
export default styles;
