import { COLOR_FONT_PRIMARY_LIGHT } from '@app/styles';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  line: {
    flex: 1,
    borderBottomWidth: 1,
    borderColor: COLOR_FONT_PRIMARY_LIGHT,
  },
});
export default styles;
